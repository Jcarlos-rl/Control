import 'package:flutter/material.dart';

class HomePageA extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Home Alumno'),
    );
  }
}